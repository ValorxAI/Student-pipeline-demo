name: Trigger auto deployment for salesforce-service-dev

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ dev ]
    paths:
    - '**'
    - '.github/workflows/salesforce-service-dev-AutoDeployTrigger-d5194457-c5e6-474a-8af8-6ed6729ae8b3.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.SALESFORCESERVICEDEV_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.SALESFORCESERVICEDEV_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.SALESFORCESERVICEDEV_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: valorxacrdev.azurecr.io
          registryUsername: ${{ secrets.SALESFORCESERVICEDEV_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.SALESFORCESERVICEDEV_REGISTRY_PASSWORD }}
          containerAppName: salesforce-service-dev
          resourceGroup: rg-valorx-dev
          imageToBuild: valorxacrdev.azurecr.io/salesforce-service-dev:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


